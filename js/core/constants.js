/**
 * constants.js
 */
export const LANGS = Object.freeze({
  PT: "pt",
  EN: "en"
});

export const THEMES = Object.freeze({
  LIGHT: "light",
  DARK:  "dark"
});

/** IDs of every <section> ordered for the nav scroll-spy. */
export const SECTION_IDS = Object.freeze([
  "hero", "about", "stack", "work", "journey", "certs", "contact"
]);
